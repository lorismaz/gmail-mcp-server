{
  "name": "@modelcontextprotocol/server-gmail",
  "version": "0.1.0",
  "description": "MCP server for interacting with Gmail",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mcp-server-gmail": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "@modelcontextprotocol/sdk": "0.5.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.3",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}